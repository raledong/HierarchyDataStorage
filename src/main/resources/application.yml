server:
  port: 8080
spring:
  datasource:
     url: jdbc:mysql://localhost:3306/tree_pattern_test?useSSL=false
     username: root
     password: root
     driver-class-name: com.mysql.jdbc.Driver
mybatis:
#  type-aliases-package: cn.deerowl
  mapper-locations: classpath:mapper/*.xml